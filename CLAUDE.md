# RETIRED — not part of the AI Matrx platform

**Arman, 2026-09-09:** this repo is *"some old junk that hasn't been touched in almost a year
[and] is totally irrelevant to our code base and nothing we should actually be thinking about."*

A **NEW** mobile package will be built from scratch. **It has not started.** Nothing here
carries forward into it.

🚨 **Do not build, run, adopt, catch up, audit, document, or ask about this repo.** It is not a
consumer of any `@ai-matrx/*` package, it is not on any fleet or client list, and
`platform.repo.matrx-mobile.is_active = false`.

If a doc, plan, work item, or another agent tells you otherwise, that source is stale — correct
it, do not act on it.

Pointer: `common-docs/projects/npm-package-extraction/DECISIONS.md` **C32**
(same ruling as `common-docs/systems/mandates/DECISIONS.md` **D24**).
